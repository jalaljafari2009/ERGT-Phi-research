# اعتبارسنجی زیرساخت پژوهش — نسخهٔ نخست

تاریخ: ۲۰۲۶-۰۹-۲۰. دامنه: ابزارهای workflow، تولید نوت‌بوک و ورود شواهد؛
آموزش علمی تازه یا اجرای Colab در این اعتبارسنجی انجام نشده است.

## آزمون‌های هدفمند

دو مجموعهٔ `tests/test_research_workflow.py` و `tests/test_colab_workflow.py`
با **۳۶ آزمون موفق** تمام شدند (۲۸ + ۸، مجموع ۹٫۱۹ ثانیه).
موارد پوشش شامل ثبت/اصلاح نسخه، gateهای ناقص یا نامعتبر، نتیجهٔ موفق و ناموفق،
هش و نسخهٔ اشتباه، مسیر ZIP ناامن، شناسهٔ تکراری، عدم بازنویسی شاهد،
داوری دارای ADR، یادآوری کار ناقص، ساخت نوت‌بوک و بازیابی بستهٔ سورس است.
آزمون‌های اتصال runner به importer، نبود ورودی/فرمان و timeout را نیز پوشش دادند.

فرمان اجرای مجدد در ریشهٔ پروژه:

```powershell
python -B -m pytest -q -p no:cacheprovider tests/test_research_workflow.py tests/test_colab_workflow.py
```

در محیط محدود ابزار، دسترسی pytest به پوشهٔ موقت Windows خطای مجوز داشت؛
اجرای آزمون با دسترسی لازم و مسیر موقت جدا موفق شد. این خطا شکست علمی یا شکست assertion نبود.

## کنترل‌های مستقل

- syntax ماژول‌ها، اسکریپت‌ها و تمام سلول‌های کد نوت‌بوک `WF-E001/v001` بررسی شد.
- نوت‌بوک منبع هیچ خروجی ازپیش‌اجراشده ندارد.
- hash مدارک هشت گروه تاریخی با `research/legacy/index.json` تطبیق داده شد.
- هر ۱۴۶ فایل بستهٔ WF-E001 با blob همان فایل در commit `cc94bcd` تطبیق داده شد.

## اجرای کامل از بستهٔ منتشرشدهٔ محلی

نمونهٔ `WF-E001/v001` در پوشهٔ خالی از ZIP قفل‌شده باز و اجرا شد.
run برابر `local-20260920T111545Z-22c8c9`، کد خروج صفر و مدت runner برابر
۴٫۰۶ ثانیه بود. هر سه gate پاس شدند و فایل اجباری مفقود نبود.
Importer نتیجه را پذیرفت؛ status نبود تفسیر و بازبینی را یادآوری کرد. پس از
نوشتن تفسیر و داوری دارای ADR، وضعیت به `passed_infrastructure` رسید.
[خلاصه](../experiments/WF-E001/v001/runs/local-20260920T111545Z-22c8c9/summary.md)،
[تفسیر](../experiments/WF-E001/v001/runs/local-20260920T111545Z-22c8c9/interpretation.md)،
[داوری](../experiments/WF-E001/v001/runs/local-20260920T111545Z-22c8c9/reviews/001.json).

تلاش قبلی `local-20260920T111511Z-49d5c0` پیش از اجرای fixture به علت مجوز
پوشهٔ موقت sandbox متوقف شد. bundle ساخته نشد؛ خطا در progress ثبت شد و همان
بسته با دسترسی عادی کاربر و شناسهٔ تازه اجرا شد. هیچ نتیجهٔ ناموفق بازنویسی نشد.

اجرای زندهٔ Colab، رفتار قطع VM و بهبود مدل ERGT در دامنهٔ این بررسی نیستند.
فهرست مقاله از همین شواهد در `research/paper/RUN_CATALOG.md` و `evidence.csv`
تولید می‌شود و شامل وضعیت اعتبار هر رکورد است.

کنترل نهایی: همهٔ پیوندهای محلی در ۲۰ فایل Markdown معتبر بودند؛ CSV سه ردیف
gate با integrity برابر `verified` و دامنهٔ infrastructure دارد. ورود دوبارهٔ
همان ZIP شناسه و فایل تازه نساخت و شاهد قبلی را تغییر نداد.
